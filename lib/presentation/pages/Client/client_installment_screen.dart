import 'package:flutter/material.dart';

class ClientInstallmentScreen extends StatelessWidget {
  const ClientInstallmentScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}